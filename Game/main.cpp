#include "GameManager.h"

// Hàm main - điểm khởi đầu của chương trình
int main() {
    // Tạo đối tượng quản lý trò chơi
    GameManager game;

    // Bắt đầu vòng lặp game
    game.run();

    return 0;
}